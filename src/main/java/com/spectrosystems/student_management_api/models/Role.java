package com.spectrosystems.student_management_api.models;

/**
 * Enum representing the roles a user can have in the system.
 * Used for authorization and assigning authorities.
 */
public enum Role {
    USER, ADMIN
}
