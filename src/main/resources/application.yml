spring:
  application:
    name: student-management-api

  datasource:
    url: jdbc:h2:mem:student_management

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  h2:
    console:
      enabled: true
